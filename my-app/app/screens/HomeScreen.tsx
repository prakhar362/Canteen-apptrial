import React from 'react';
import { View, Text } from 'react-native';


const HomeScreen = () => {
  return (
    <View className={`flex-1 justify-center items-center`}>
      <Text className={`text-xl font-bold`}>Welcome to the Canteen App</Text>
    </View>
  );
};

export default HomeScreen;
